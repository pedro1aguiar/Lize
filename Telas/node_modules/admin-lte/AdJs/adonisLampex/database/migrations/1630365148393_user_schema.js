'use strict'

/** @type {import('@adonisjs/lucid/src/Schema')} */
const Schema = use('Schema')

class UserSchema extends Schema {
  up () {
    this.create('users', (table) => {
      table.increments()
      table.uuid('secure_id').unique();
      table.string('NomeDaEmpresa', 35).notNullable();
      table.string('CNPJ').notNullable();
      table.string('Endereco', 50).notNullable();
      table.string('Email', 50).notNullable();
      table.string('RespContrato', 50).notNullable();
      table.string('TelRespContrato').notNullable();
      table.string('RespProjeto', 50).notNullable();
      table.string('TelRespProjeto').notNullable();
      table.date("delete_at");
      table.timestamps();
    });
  }

  down () {
    this.drop('users')
  }
}

module.exports = UserSchema
